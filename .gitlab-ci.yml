image: python:3.11-slim

stages:
  - test

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  OMP_NUM_THREADS: "1"
  MKL_NUM_THREADS: "1"

cache:
  key:
    files:
      - requirements.txt
  paths:
    - .cache/pip

tests:
  stage: test
  before_script:
    - python -m pip install --upgrade pip
    - python -m pip install -r requirements.txt
    # pandas is the only optional dependency the test suite imports for real;
    # laptrack and ultralytics are stubbed in the tests, so they are not needed.
    - python -m pip install pandas
  script:
    - python -m pytest -q
